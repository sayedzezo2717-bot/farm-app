# 🏗️ مخطط الهندسة المعمارية التفصيلي

## المكونات الرئيسية

```
┌─────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                        │
│  ┌──────────────────┐  ┌──────────────────┐  ┌────────────┐ │
│  │  BatchSetup      │  │  Broiler         │  │   Layer    │ │
│  │  Screen          │  │  Dashboard       │  │  Dashboard │ │
│  └──────────────────┘  └──────────────────┘  └────────────┘ │
│  ┌──────────────────────────────────────────────────────┐   │
│  │           Daily Record Input Screen (WIP)            │   │
│  └──────────────────────────────────────────────────────┘   │
└────────────────────┬─────────────────────────────────────────┘
                     │
┌────────────────────▼─────────────────────────────────────────┐
│                    BUSINESS LOGIC LAYER                       │
│  ┌────────────────────────────────────────────────────────┐  │
│  │         LocalStorageService (Singleton)               │  │
│  │  - CRUD Operations                                    │  │
│  │  - Batch Management                                  │  │
│  │  - Daily Records                                     │  │
│  │  - Statistical Calculations                          │  │
│  └────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────┐  │
│  │         Analytics Engine (قريب)                      │  │
│  │  - Performance Calculations                          │  │
│  │  - Predictions                                       │  │
│  │  - Health Warnings                                   │  │
│  └────────────────────────────────────────────────────────┘  │
└────────────────────┬─────────────────────────────────────────┘
                     │
┌────────────────────▼─────────────────────────────────────────┐
│                    DATA ACCESS LAYER                          │
│  ┌─────────────────────┐  ┌──────────────────────────────┐  │
│  │  Hive              │  │  SharedPreferences           │  │
│  │  (Complex Data)    │  │  (Simple Key-Value)          │  │
│  │                     │  │                              │  │
│  │ - BatchConfig      │  │ - Active Batch ID            │  │
│  │ - DailyRecord      │  │ - User Preferences           │  │
│  │ - CageBattery      │  │ - App Settings               │  │
│  └─────────────────────┘  └──────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

---

## تدفق البيانات

### 1️⃣ إنشاء دورة جديدة:

```
User Input (Setup Screen)
    ↓
Validation Layer
    ├─ Check Required Fields ✓
    ├─ Verify Total Birds = Sum of Cages ✓
    ├─ Check Stocking Density (8-15/m²) ✓
    └─ Validate Age Range ✓
    ↓
Data Model Creation
    └─ BatchConfig Object
    ├─ Generate UUID
    ├─ Calculate Initial Age in Days
    └─ Set Current Timestamp
    ↓
Persistence Layer
    ├─ Save to Hive
    └─ Set as Active Batch (SharedPreferences)
    ↓
Navigation
    └─ Redirect to Dashboard
```

### 2️⃣ تحديث البيانات اليومية:

```
Daily Record Input
    ↓
Type-Specific Validation
    ├─ Broiler: Weight, Feed Consumption
    └─ Layer: Egg Production, Production %
    ↓
Record Creation
    └─ DailyRecord Object with calculated fields
    ├─ FCR (Feed Conversion Ratio) for Broiler
    └─ Eggs per Bird for Layer
    ↓
Storage
    └─ Save to Hive (Daily Records Box)
    ↓
Dashboard Update
    └─ Refresh Statistics & KPIs
```

### 3️⃣ حساب الإحصائيات:

```
Query Request from Dashboard
    ↓
Retrieve Data from Hive
    ├─ Get All Records for Batch
    └─ Filter by Type if needed
    ↓
Calculate Metrics
    ├─ Broiler:
    │  ├─ Avg Weight
    │  ├─ Total Feed Consumption
    │  ├─ Feed Conversion Ratio
    │  └─ Mortality Rate
    └─ Layer:
       ├─ Total Egg Production
       ├─ Avg Production %
       └─ Mortality Rate
    ↓
Generate KPI Cards
    └─ Display in Dashboard
```

---

## نماذج البيانات والعلاقات

```
┌─────────────────────────────────────┐
│         ProductionType (Enum)       │
│  ├─ BROILER                         │
│  └─ LAYER                           │
└────────────┬────────────────────────┘
             │ 1
             │
             │ has many
             │
┌────────────▼────────────────────────┐
│          Breed                       │
│  ├─ id: String                       │
│  ├─ name: String                     │
│  ├─ type: ProductionType             │
│  ├─ avgDailyGain: double (Broiler)  │
│  ├─ peakEggProduction: double (Layer)│
│  └─ nutritionRequirements: Map       │
└────────────────────────────────────┘

┌─────────────────────────────────────┐
│         BatchConfig                  │
│  ├─ id: String (UUID)                │
│  ├─ productionType: Enum             │
│  ├─ breedId: String (FK)             │
│  ├─ startDate: DateTime              │
│  ├─ initialAge: int                  │
│  ├─ totalBirdCount: int              │
│  ├─ cages: List<CageBattery>         │
│  ├─ farmName: String                 │
│  └─ notes: String                    │
└────────────┬────────────────────────┘
             │ 1
             │
             │ has many
             │
┌────────────▼────────────────────────┐
│       CageBattery                    │
│  ├─ id: String (UUID)                │
│  ├─ name: String                     │
│  ├─ birdCount: int                   │
│  └─ cageArea: double                 │
└────────────────────────────────────┘

             ▲
             │ 1
             │
             │ has many
             │
┌────────────┴────────────────────────┐
│         DailyRecord                  │
│  ├─ id: String (UUID)                │
│  ├─ batchId: String (FK)             │
│  ├─ date: DateTime                   │
│  ├─ averageWeight: double? (Broiler) │
│  ├─ feedConsumption: double?         │
│  ├─ eggProduction: int? (Layer)      │
│  ├─ eggProductionPercentage: double? │
│  ├─ deadCount: int?                  │
│  └─ notes: String                    │
└─────────────────────────────────────┘
```

---

## خدمة التخزين المحلي - التفاصيل

### Hive Boxes:

```
Hive Database
├── batch_config (Box<BatchConfig>)
│   └─ Contains all active and completed batches
│       Key: batch.id
│       Value: BatchConfig object
│
└── daily_records (Box<DailyRecord>)
    └─ Contains all daily measurements
        Key: record.id
        Value: DailyRecord object
```

### SharedPreferences Keys:

```
SharedPreferences
├── active_batch_id: String
│   └─ Stores current active batch ID
│
├── user_preferences: JSON
│   └─ Theme, Language, Units (future)
│
└── app_settings: JSON
    └─ Notification settings, etc
```

### Adapters (للتسلسل):

```
Hive Adapters Registration
├── TypeId: 0 → CageBatteryAdapter
├── TypeId: 1 → BatchConfigAdapter
├── TypeId: 2 → DailyRecordAdapter
└── TypeId: 3 → ProductionTypeAdapter
```

---

## معالجة الأخطاء

```
┌─────────────────────────────────────┐
│        Try-Catch Hierarchy          │
└─────────────────────────────────────┘
              │
        ┌─────▼─────┐
        │ UI Layer  │
        └─────┬─────┘
              │
        ┌─────▼─────────────────┐
        │ Validation Errors     │
        ├─ Display User Message │
        └─────────────────────┘

        ┌─────────────────────────┐
        │ Service Layer (Storage) │
        ├─ Log Errors            │
        └────────┬────────────────┘
                 │
        ┌────────▼────────────┐
        │ Database Errors     │
        ├─ Hive/Prefs Issues  │
        └────────┬────────────┘
                 │
        ┌────────▼────────────┐
        │ Fallback Actions:   │
        ├─ Retry Logic        │
        ├─ Clear Cache        │
        └─ Notify User        │
```

---

## الحالات الخاصة والتحقق

### التحقق من الكثافة:

```
Stocking Density Validation:

┌─────────────────────────────────┐
│  Input: birdCount, cageArea     │
└────────────┬────────────────────┘
             │
      ┌──────▼──────┐
      │ Calculate:  │
      │ density =   │
      │ birds/area  │
      └──────┬──────┘
             │
    ┌────────▼────────┐
    │  Check Range:   │
    │  8 < D < 15     │
    └────────┬────────┘
             │
    ┌────────▼───────────────┐
    │ Result:                │
    ├─ Safe: ✓              │
    └─ Unsafe: ✗ Warn User  │
```

### حساب الوزن المتوقع (اللحم):

```
Expected Weight Calculation:

Age (days) × Daily Gain Rate = Expected Weight

Example: Cobb Broiler
├─ Day 1: 1 × 2.15g = 2.15g
├─ Day 7: 7 × 2.15g = 15.05g
├─ Day 21: 21 × 2.15g = 45.15g
├─ Day 35: 35 × 2.15g = 75.25g
└─ Day 42: 42 × 2.15g = 90.30g (target)

If Actual < Expected × 0.9 → Warning ⚠️
```

### حساب نسبة التحويل الغذائي:

```
Feed Conversion Ratio (FCR):

FCR = Total Feed Consumed (kg)
      ─────────────────────────
      Total Weight Gained (kg)

Example:
├─ Feed Consumed: 100 kg
├─ Birds: 1000
├─ Current Weight: 2.5 kg each
├─ Total Weight: 2500 kg
├─ FCR = 100 / 2.5 = 40:1 ❌ (Bad)

Better Example:
├─ Feed: 100 kg
├─ Weight Gain: 2500 kg
├─ FCR = 100 / 2500 = 0.04 (Good ✓)
```

---

## التوسعات المستقبلية

### المرحلة 2:

```
┌────────────────────────┐
│ Analytics Engine       │
├─ Performance Trends    │
├─ Predictive Analytics  │
└─ Cost Analysis         │
```

### المرحلة 3:

```
┌────────────────────────┐
│ Cloud Synchronization  │
├─ Firebase             │
├─ Backup & Restore     │
└─ Multi-device Sync    │
```

### المرحلة 4:

```
┌────────────────────────┐
│ Advanced Features      │
├─ IoT Integration       │
├─ Automated Alerts      │
├─ Feed Planning AI      │
└─ Mobile App Clone      │
```

---

## قائمة التحقق من التنفيذ

### المرحلة 1: الإعداد الأساسي ✓
- [x] نماذج البيانات
- [x] Hive Adapters
- [x] LocalStorageService
- [x] التحقق من الصحة

### المرحلة 2: الواجهات ✓
- [x] BatchSetupScreen
- [x] DashboardScreen (Broiler + Layer)
- [x] Navigation & Routing

### المرحلة 3: الميزات الإضافية
- [ ] DailyRecordScreen
- [ ] Analytics Dashboard
- [ ] Report Generation
- [ ] Notifications

### المرحلة 4: التحسينات
- [ ] Dark Theme Support
- [ ] Localization (AR/EN)
- [ ] Offline Sync
- [ ] Cloud Backup

---

## الملفات والمسارات

```
farm_management_app/
├── lib/
│   ├── main.dart                    # نقطة الدخول
│   ├── models.dart                  # نماذج البيانات
│   ├── local_storage_service.dart   # الخدمة
│   ├── batch_setup_screen.dart      # إعداد الدورة
│   ├── dashboard_screen.dart        # لوحة التحكم
│   ├── examples.dart                # أمثلة
│   └── pages/
│       ├── daily_record_page.dart   # سجل يومي (قريب)
│       └── reports_page.dart        # التقارير (قريب)
│
├── assets/
│   ├── images/
│   ├── icons/
│   └── fonts/
│
├── test/
│   ├── models_test.dart
│   └── storage_service_test.dart
│
├── pubspec.yaml                     # المكتبات
├── DOCUMENTATION.md                 # الوثائق
└── ARCHITECTURE.md                  # هذا الملف
```

---

**النسخة:** 1.0
**آخر تحديث:** يناير 2026
